<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.panagiotiswarpro.panos.businessplan">

    <application
        android:allowBackup="true"
        android:icon="@drawable/idea"
        android:label="@string/app_name"
        android:roundIcon="@drawable/idea"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BusinessCreationScreen" />
        <activity
            android:name=".BusinessModel"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Equipment"
            android:label="@string/title_activity_equipment"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Team"
            android:label="@string/title_activity_team"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ProiontaTab"
            android:label="@string/title_activity_proionta_tab"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

</manifest>